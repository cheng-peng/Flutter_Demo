import 'package:flutter/material.dart';

class FlutterLayoutDemo extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
        child: Text('FlutterLayoutDemo'),
    );
  }
}